[package]
name = "experimental"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]
test=false
bench=false

[profile.release]
panic = "abort"
lto = true

[dependencies]
fbsd-kdev = { path = "../../fbsd-kdev" }
